# Secrets / env
.env
.env.local
*.key
*.pem

# Local compose overrides (infra-specific: networks, host paths, ports)
compose.override.yaml
compose.override.yml

# Dependencies
node_modules/

# Build output
dist/
backend/dist/
frontend/dist/

# Runtime data (never in the repo — bind-mounted into the container at /data)
data/
albums/
tmp/
*.db
*.db-wal
*.db-shm

# Logs / OS cruft
*.log
.DS_Store
Thumbs.db
